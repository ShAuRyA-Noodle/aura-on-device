# Aura local-network FastAPI bridge.
# Production-grade Mac-side daemon for the iPhone client.

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
structlog>=24.0
slowapi>=0.1.9
prometheus-client>=0.20
typer>=0.12
websockets>=12.0
jsonschema>=4.23
python-dateutil>=2.9

# Test extras
httpx>=0.27
pytest>=8.0
pytest-asyncio>=0.23

# Optional: QR code rendering for `aura serve --show-token`.
qrcode>=8.2

# Optional: load testing.
locust>=2.20

# Diff helpers used in tests.
deepdiff>=7.0
