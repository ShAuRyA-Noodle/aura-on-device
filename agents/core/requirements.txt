# Aura — Python reference implementation pinned deps.
# Mirrors technical_spec.md §13.3 with the four packages this owner needs.
# Note: heavy ML deps (mlx, mlx-lm) are dev-only on Apple Silicon — not pinned
# here so CI runs on Linux without forcing Metal.

pydantic==2.13.4
langgraph>=1.2.6
langchain-core>=0.3.85
jsonschema==4.26.0
pytest>=8.3.3
pytest-asyncio>=0.24.0
sentence-transformers==5.6.0
transformers>=5.12.1
peft==0.19.1
llama-cpp-python==0.3.23
python-dateutil==2.9.0.post0
