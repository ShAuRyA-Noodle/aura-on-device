# CI-only slim dependency set.
# Excludes heavy ML packages (sentence-transformers, transformers, peft,
# llama-cpp-python) that the runtime needs but the test suite does not.
# llama-cpp-python in particular requires C++ build tooling that breaks
# free-tier GitHub Actions runners on cold cache.
#
# The agent + orchestrator + memory tests import only the lightweight
# packages below; ML calls are stubbed.

pydantic==2.13.4
jsonschema==4.26.0
pytest>=8.3.3
pytest-asyncio==0.24.0
python-dateutil==2.9.0.post0

# CI needs these too — they're used by the test code paths in
# agents/comms/classifier.py (numpy + scikit-learn for the LR baseline),
# agents/wellness/load_score.py (xgboost), and a couple of small
# statistical helpers in the orchestrator (scipy.stats).
numpy>=2.5.0
scikit-learn>=1.4
scipy>=1.11
xgboost>=2.0
deepdiff>=9.1.0
